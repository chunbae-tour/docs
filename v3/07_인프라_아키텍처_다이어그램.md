# 07_인프라_아키텍처_다이어그램

<!-- latest-update-2026-06 -->

> **문서 버전**: v3.0  
> **최신 반영일**: 2026-06-22  
> **최종 운영 구조**: GitHub Actions → ECR → ECS Fargate → ALB → RDS/ElastiCache/S3

## 0. 최종 인프라 반영 사항

기존 초기 설계는 EC2 Docker 배포를 중심으로 작성되었으나, 최종 운영 구조는 ECS Fargate 기반으로 전환되었다. 따라서 본 문서에서는 아래 최신 구조를 우선 기준으로 본다.

```mermaid
flowchart LR
    Client[Client / Frontend] --> DNS[www.chunbae-tour.site]
    Client --> API[api.chunbae-tour.site]
    API --> ALB[Application Load Balancer]
    ALB --> ECS[ECS Fargate Service]
    ECS --> RDS[(RDS MySQL)]
    ECS --> Redis[(ElastiCache Redis Cluster)]
    ECS --> S3[(AWS S3)]
    ECS --> Secrets[AWS Secrets Manager]
    ECS --> Kakao[Kakao Local / Map Link]
    ECS --> PortOne[PortOne V2]
    ECS --> PublicAPI[Tour API / 공공데이터포털]
    ECS --> Google[Google Translation API]
    ECS --> CW[CloudWatch Logs]
    ECS --> Actuator[Actuator / Micrometer]
    Actuator --> ADOT[ADOT Collector]
```

### 배포 흐름

```mermaid
flowchart LR
    PR[Pull Request] --> Test[Gradle Test / Compile]
    Main[main merge] --> Build[Docker image build]
    Build --> ECR[Push to ECR]
    ECR --> Deploy[ECS rolling deployment]
    Deploy --> Health[ALB health check]
    Health --> Live[Production traffic]
    Health --> Rollback[ECS circuit breaker rollback]
```

| 구성 요소 | 최종 역할 |
| --- | --- |
| GitHub Actions | 테스트 게이트, Docker 이미지 빌드, ECR push, ECS 배포 자동화 |
| ECR | 백엔드 Docker 이미지 저장소 |
| ECS Fargate | Spring Boot 컨테이너 실행, 서버 직접 관리 제거 |
| ALB | HTTPS 트래픽 수신, target health 기반 라우팅 |
| RDS MySQL | 영속 데이터 저장, Flyway migration 대상 |
| ElastiCache Redis Cluster | 캐시, 랭킹, 최근 검색어, Geo, Pub/Sub, 분산 락, write-behind counter |
| S3 | 프로필/게시글/채팅/CS/가게 이미지 및 첨부 저장 |
| Secrets Manager | DB, Redis, OAuth, JWT, 외부 API secret 관리 |
| CloudWatch | 애플리케이션 로그, 배포 장애 추적 |
| Actuator/Micrometer/ADOT | health check와 metric 수집 |

### Redis Cluster 운영 메모

ElastiCache Redis는 Cluster Mode Enabled 기준이다. 따라서 `RENAME`, `MGET` 같은 multi-key 명령은 같은 hash slot의 key에서만 안전하다. 인기 검색어 스냅샷/오타 교정/추천 캐시는 hash tag로 key slot을 맞추고, 관광지 통계처럼 key 수가 많은 경우에는 hot slot을 만들지 않도록 개별 GET/pipeline 전략을 사용한다.

---


> **문서 버전**: v1.0
**작성일**: 2026-05-15
**프로젝트명**: 춘배투어 (ChunBae Tour)
**작성자**: 황춘배
> 

---

## 1. 전체 인프라 구성 개요

| 구성 요소 | 서비스 | 사양 | 역할 |
| --- | --- | --- | --- |
| **애플리케이션 서버** | AWS EC2 | t3.medium | Spring Boot 앱 실행 (Docker) |
| **데이터베이스** | AWS RDS MySQL | db.t3.micro | 메인 데이터 저장소 (AWS RDS 접근 포트: 3306) |
| **캐시 / 메시징** | AWS ElastiCache Redis | cache.t3.micro | 캐시, 분산 락, Pub/Sub, Geospatial |
| **컨테이너 레지스트리** | AWS ECR | - | Docker 이미지 저장소 |
| **CI/CD** | GitHub Actions | - | 빌드 → ECR Push → EC2 배포 |
| **프론트엔드** | React PWA | - | 바이브코딩 (정적 파일 서빙 or S3) |
| **외부 연동** | KakaoMap API / PG사 / Google Cloud Translation API / Tour API(추후 확장) | - | 외부 서비스 연동 |

---

## 2. 전체 인프라 아키텍처 다이어그램

> ECS Fargate 기반 최종 운영 구조 (EC2 구조에서 전환됨)

```mermaid
flowchart TD
    Client["👤 사용자 브라우저\n(React PWA)"]

    subgraph CICD["⚙️ CI/CD (GitHub Actions)"]
        Repo["GitHub Repository"]
        Runner["GitHub Actions Runner"]
        Repo -->|"Push/PR 트리거"| Runner
    end

    subgraph AWS["AWS (ap-northeast-2 · 서울 리전)"]
        ECR["📦 ECR\nDocker Image Registry"]

        SGALB["🔒 SG-ALB\n80, 443"]
        SGTASK["🔒 SG-TASK\n8080 (ALB만)"]
        SGRDS["🔒 SG-RDS\n3306 (TASK만)"]
        SGREDIS["🔒 SG-Redis\n6379 (TASK만)"]

        subgraph VPC["VPC (가상 사설 네트워크)"]
            subgraph PubSub["Public Subnet (NAT 미사용)"]
                ALB["Application Load Balancer\napi.chunbae-tour.site · ACM"]
                Task["🖥️ ECS Fargate Task\nSpring Boot App (8080)"]
            end
            subgraph PrivSub["Private Subnet"]
                RDS[("RDS MySQL\nMulti-AZ")]
                Redis[("ElastiCache Redis\nMulti-AZ")]
            end
        end
    end

    subgraph Ext["🌐 외부 서비스"]
        Kakao["KakaoMap API (길찾기)"]
        PG["PG사 (결제)"]
        EtcAPI["번역·공공데이터 API"]
    end

    Client -->|"HTTPS 443"| ALB
    ALB -->|"8080"| Task
    Task -->|"3306 (VPC 내부)"| RDS
    Task -->|"6379 (VPC 내부)"| Redis
    Task -->|"Image Pull"| ECR
    Task -->|"HTTPS"| Kakao
    Task -->|"HTTPS"| PG
    Task -->|"HTTPS"| EtcAPI

    Runner -->|"Docker Image Push"| ECR
    Runner -->|"ECS 서비스 업데이트\n(롤링 배포 · AWS API)"| Task

    SGALB -.->|"적용"| ALB
    SGTASK -.->|"적용"| Task
    SGRDS -.->|"적용"| RDS
    SGREDIS -.->|"적용"| Redis

    classDef node fill:#EDEAFB,stroke:#9D8DF1,color:#26251F;
    classDef store fill:#E3F2E8,stroke:#4CA46A,color:#26251F;
    classDef sg fill:#FBE9E7,stroke:#D98C7A,color:#26251F;
    class Client,Repo,Runner,ECR,ALB,Task,Kakao,PG,EtcAPI node;
    class RDS,Redis store;
    class SGALB,SGTASK,SGRDS,SGREDIS sg;
    style AWS fill:#FFFDF2,stroke:#D9C84B;
    style VPC fill:#FBF8E0,stroke:#D9C84B;
    style PubSub fill:#FFFFFF,stroke:#C9B84B;
    style PrivSub fill:#F4F1FF,stroke:#B9A9F1;
    style CICD fill:#F0F4FA,stroke:#9BB3D9;
    style Ext fill:#F0F4FA,stroke:#9BB3D9;
```

---

## 3. CI/CD 파이프라인 상세

> ECS Fargate 롤링 배포 기준 (SSH 없음)

```mermaid
sequenceDiagram
    autonumber
    participant Dev as 개발자
    participant GH as GitHub
    participant GA as GitHub Actions
    participant ECR as AWS ECR
    participant ECS as AWS ECS (Fargate)
    participant ALB as ALB

    Dev->>GH: git push (main 브랜치)
    GH->>GA: Workflow 트리거

    rect rgb(235, 240, 255)
    Note over GA: CI 단계
    GA->>GA: ./gradlew build (빌드 + 테스트)
    GA->>GA: 빌드 실패 시 중단 + Slack 알림
    end

    rect rgb(234, 250, 240)
    Note over GA,ECR: Docker 이미지 빌드 & Push
    GA->>GA: docker build -t chunbae-tour .
    GA->>ECR: docker push (태그: latest + commit SHA)
    end

    rect rgb(255, 244, 235)
    Note over GA,ALB: CD 단계 — ECS 롤링 배포 (SSH 없음)
    GA->>ECS: 서비스 업데이트 (새 태스크 정의 · AWS API)
    ECS->>ECR: docker pull (새 이미지)
    ECS->>ECS: 새 Fargate 태스크 기동
    ECS->>ALB: 타겟 등록 (헬스체크)
    ALB-->>ECS: 헬스체크 통과
    ECS->>ECS: 기존 태스크 드레인 후 종료
    Note over ECS: 실패 시 서킷브레이커 자동 롤백
    ECS-->>GA: 배포 완료 (서비스 안정화)
    end

    GA->>Dev: 배포 완료 알림 (Slack)
```

---

## 4. 네트워크 보안 구성 (Security Group)

> SSH 포트(22) 없음 — 배포는 GitHub Actions OIDC → AWS API 경유(SSH 키 불필요)

```mermaid
graph LR
    subgraph INTERNET["인터넷"]
        USER["사용자"]
        GH_ACTIONS["GitHub Actions\n(OIDC)"]
    end

    subgraph VPC["AWS VPC"]
        subgraph SG_ALB["SG-ALB"]
            ALB["ALB"]
        end
        subgraph SG_TASK["SG-TASK"]
            TASK["ECS Fargate Task\n(chunbae-tour)"]
        end
        subgraph SG_RDS["SG-RDS"]
            RDS[("RDS MySQL")]
        end
        subgraph SG_REDIS["SG-Redis"]
            REDIS[("ElastiCache Redis")]
        end
    end

    USER -->|"80, 443"| ALB
    GH_ACTIONS -->|"AWS API (ECS 배포)"| TASK
    ALB -->|"8080 (SG-TASK만)"| TASK
    TASK -->|"3306 (SG-TASK만)"| RDS
    TASK -->|"6379 (SG-TASK만)"| REDIS
```

### Security Group 규칙 상세

**SG-ALB (로드밸런서)**

| 방향 | 프로토콜 | 포트 | 소스 | 설명 |
| --- | --- | --- | --- | --- |
| Inbound | TCP | 80 | 0.0.0.0/0 | HTTP → HTTPS 리다이렉트 |
| Inbound | TCP | 443 | 0.0.0.0/0 | HTTPS (ACM 인증서) |
| Outbound | TCP | 8080 | SG-TASK | 앱 트래픽 전달 |

**SG-TASK (ECS Fargate 태스크)**

| 방향 | 프로토콜 | 포트 | 소스 | 설명 |
| --- | --- | --- | --- | --- |
| Inbound | TCP | 8080 | SG-ALB | ALB에서만 앱 요청 수신 |
| Outbound | ALL | ALL | 0.0.0.0/0 | 외부 API / ECR / Secrets Manager 호출 |

> 포트 9090(Actuator 관리 포트)은 태스크 내부 헬스체크(`wget localhost:9090/actuator/health`)만 사용 — 외부 미노출

**SG-RDS (데이터베이스)**

| 방향 | 프로토콜 | 포트 | 소스 | 설명 |
| --- | --- | --- | --- | --- |
| Inbound | TCP | 3306 | SG-TASK | ECS 태스크에서만 접근 허용 |

**SG-Redis (ElastiCache)**

| 방향 | 프로토콜 | 포트 | 소스 | 설명 |
| --- | --- | --- | --- | --- |
| Inbound | TCP | 6379 | SG-TASK | ECS 태스크에서만 접근 허용 |

---

## 5. 애플리케이션 내부 요청 처리 흐름

> Nginx 없음 — ALB가 TLS 종료, 태스크에 8080 직접 전달. 컨테이너 2개(앱 + ADOT 사이드카).

```mermaid
graph TD
    REQUEST["클라이언트 요청\n(HTTPS)"] --> ALB_NODE["ALB\n(TLS 종료 · ACM)"]

    subgraph TASK["ECS Fargate Task"]
        subgraph APP_CONTAINER["chunbae-tour 컨테이너\n(8080 앱 / 9090 Actuator)"]
            FILTER["Security Filter Chain\n(JWT 검증)"]
            CONTROLLER["Controller Layer"]
            SERVICE["Service Layer"]
            REPO["Repository Layer"]
        end
        ADOT["adot-collector 사이드카\n(메트릭 수집)"]
    end

    SECRETS["AWS Secrets Manager\n(DB·Redis·OAuth·PG 시크릿)"]
    CW["CloudWatch Logs\n(/ecs/chunbae-tour)"]

    ALB_NODE -->|"8080"| FILTER
    FILTER -->|"인증 통과"| CONTROLLER
    CONTROLLER --> SERVICE
    SERVICE -->|"일반 조회"| REPO

    REPO -->|"JPA / QueryDSL"| RDS_EXT[("RDS MySQL")]
    SERVICE -->|"캐시 / 락 / Geo / Pub-Sub"| REDIS_EXT[("ElastiCache Redis\nCluster Mode")]
    SERVICE -->|"이미지 업로드·다운로드"| S3["AWS S3"]
    SERVICE -->|"HTTPS"| EXT["외부 서비스\n(KakaoMap / PortOne / Google Translation\n/ 공공데이터포털 TourAPI)"]

    TASK -->|"시작 시 시크릿 주입"| SECRETS
    APP_CONTAINER -->|"stdout 로그"| CW
    APP_CONTAINER -->|"Actuator 메트릭 (9090)"| ADOT
    ADOT -->|"stdout 로그"| CW
```

---

## 6. WebSocket 실시간 채팅 흐름

```mermaid
graph LR
    subgraph CLIENTS["클라이언트"]
        UA["User A
        브라우저"]
        UB["User B
        브라우저"]
    end

    subgraph ECS_TASK["ECS Fargate Task (Spring Boot)"]
        WS["WebSocket STOMP Handler"]
        BROKER["In-Memory Message Broker"]
    end

    REDIS_PS[("ElastiCache
    Redis Pub/Sub")]
    DB[("RDS
    MySQL 메시지 저장")]

    UA -->|"WebSocket 연결 /ws-stomp
구독 /sub/chat/rooms/{chatRoomId}"| WS
    UB -->|"WebSocket 연결 /ws-stomp
구독 /sub/chat/rooms/{chatRoomId}"| WS

    UA -->|"메시지 발행 /pub/chat/rooms/{chatRoomId}/messages"| WS
    WS --> BROKER
    BROKER -->|"Publish"| REDIS_PS
    REDIS_PS -->|"Subscribe → Push"| BROKER
    BROKER -->|"메시지 전달"| UA
    BROKER -->|"메시지 전달"| UB
    WS -->|"메시지 저장
(최근 메시지 조회 중심, 영구 보관 전제 아님)"| DB
```

> 고객센터 실시간 상담 채팅도 동일한 WebSocket/STOMP 인프라를 사용하되, STOMP 경로는 `/pub/support/rooms/{supportRoomId}/messages`, `/sub/support/rooms/{supportRoomId}`로 분리한다.
> 

> **💡 Redis Pub/Sub을 쓰는 이유**
ECS 태스크가 여러 개로 수평 확장될 경우, 태스크 A에 연결된 User A가 보낸 메시지를
태스크 B에 연결된 User B도 받을 수 있도록 Redis가 브로커 역할을 한다.
현재 단일 태스크 운영 중이지만 확장을 고려한 구조로 설계됨.
> 

---

## 7. 환경 분리 전략

| 환경 | 트리거 | 배포 대상 | DB | 프로파일 |
| --- | --- | --- | --- | --- |
| **local** | 로컬 실행 | Docker Compose (MySQL+Redis만) | 로컬 MySQL 8.4 (포트 3308) | `local` |
| **prod** | main 브랜치 push | ECS Fargate (롤링 배포) | AWS RDS MySQL (Multi-AZ) | `prod` |

- CI (`ci.yml`): `feature/*` PR → develop, develop push → 컴파일 게이트 + Testcontainers 테스트
- CD (`cd.yml`): main push → 테스트 재게이트 → ECR push(commit SHA) → ECS rolling 배포

> 로컬 MySQL은 호스트 3308(컨테이너 내부 3306), Redis는 호스트 6380(컨테이너 내부 6379) 포트 매핑 사용.
> `.env.example` → `.env` 복사 후 `scripts/dev-up.sh` (또는 `dev-up.ps1`) 실행.

### Docker Compose (로컬 개발 환경)

```yaml
# docker-compose.yml (로컬 개발용 — MySQL+Redis만, 앱은 IDE에서 직접 실행)
services:
  mysql:
    image: mysql:8.4
    container_name: chunbae-tour-mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-1234}
      MYSQL_DATABASE: ${DB_NAME:-chunbae_tour}
      MYSQL_USER: ${DB_USERNAME:-chunbae}
      MYSQL_PASSWORD: ${DB_PASSWORD:-1234}
    ports:
      - "${DB_PORT:-3308}:3306"   # 호스트 3308 → 컨테이너 3306
    volumes:
      - chunbae-mysql-data:/var/lib/mysql

  redis:
    image: redis:7-alpine
    container_name: chunbae-tour-redis
    ports:
      - "${REDIS_PORT:-6380}:6379"  # 호스트 6380 → 컨테이너 6379
    volumes:
      - chunbae-redis-data:/data

volumes:
  chunbae-mysql-data:
  chunbae-redis-data:
```

---

## 8. 인프라 비용 추정 (월 기준)

| 서비스 | 사양 | 예상 비용 |
| --- | --- | --- |
| ECS Fargate (태스크 1개 상시) | 1 vCPU · 2GB RAM | ~$41/월 |
| ALB | LCU 기준 | ~$18/월 |
| RDS MySQL (Multi-AZ) | db.t3.micro | ~$30/월 |
| ElastiCache Redis (Cluster) | cache.t3.micro | ~$20/월 |
| ECR | 이미지 스토리지 | ~$1/월 |
| AWS Secrets Manager | ~25개 시크릿 | ~$10/월 |
| CloudWatch Logs | 로그 수집·보관 | ~$3/월 |
| S3 | 이미지 스토리지 | ~$1/월 |
| **합계** | | **~$124/월** |

> ⚠️ AWS 교육 계정 크레딧 사용 시 비용 부담 없음.
> Fargate는 태스크 실행 시간 기준 과금 — ECS 서비스 desired count=0으로 내리면 즉시 중단 가능.
> RDS·ElastiCache는 인스턴스 중지 후에도 스토리지 비용 발생.

---

*본 문서는 실제 배포 구성 변경 시 함께 업데이트한다.*
